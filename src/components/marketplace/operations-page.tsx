import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
export function OperationsPage({ title, eyebrow, description }: { title: string; eyebrow: string; description: string }) { return <div className="space-y-6"><div><p className="font-mono text-xs uppercase tracking-widest text-primary">{eyebrow}</p><h1 className="mt-2 font-heading text-3xl font-semibold">{title}</h1><p className="mt-2 text-sm text-muted-foreground">{description}</p></div><Card><CardHeader><CardTitle>Nothing to review yet</CardTitle></CardHeader><CardContent className="text-sm text-muted-foreground">Live records will appear here once the marketplace database is connected and populated.</CardContent></Card></div>; }
